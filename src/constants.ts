export enum HTTP_CODE {
    OK = 200,
    CREATED = 201,
    ACCEPTED = 202,
    SERVER_ERROR = 500,
    BAD_REQUEST = 400,
    NOT_FOUND = 404,
}

export enum TIMING {
    SECOND = 1000,
    MINUTE = 60000,
    HOUR = 3600000,
    DAY = 86400000
}
